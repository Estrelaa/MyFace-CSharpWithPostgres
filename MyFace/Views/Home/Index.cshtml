@{
    ViewBag.Title = "Home :: Galaxy";;
}

    <div class="limited-width-container">
        <h1>Welcome to Galaxy</h1>
        <!--TODO Login should redirect to a login page-->
        @Html.ActionLink($"Login", "Index", "UserList", "", new { @class = "button" })
        @Html.ActionLink($"SignUp", "SignUp", "Account", "", new { @class = "button" })
    </div>